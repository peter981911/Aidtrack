services:
  backend:
    build:
      context: ./aidtrack-backend
      dockerfile: Dockerfile
    container_name: aidtrack-backend
    ports:
      - "5000:5000"
    env_file:
      - ./aidtrack-backend/.env
    restart: unless-stopped

  frontend:
    build:
      context: ./aidtrack_frontend
      dockerfile: Dockerfile
    container_name: aidtrack-frontend
    ports:
      - "80:80"
    depends_on:
      - backend
    restart: unless-stopped
